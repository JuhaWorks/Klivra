# VITE_API_URL=https://syncforge-io.onrender.com
# ^ Commented out for local development so Vite uses the local proxy (/api -> localhost:5000).
# Add this variable in your Vercel project settings instead.
